esphome:
  name: study-temp

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:
  password: "cb141b540ca5f0dae057151ffa0d4f74"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Study-Temp Fallback Hotspot"
    password: "nG4q3eIGByZL"

captive_portal:
